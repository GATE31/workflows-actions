name: fff
on: push

jobs:
  docker-build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Git clone repository
        uses: actions/checkout@v3
      - name: Get-npm-version
        id: package-version
        uses: martinbeentjes/npm-get-version-action@main
      - name: use my action
        uses: GATE31/workflows-actions@v1
